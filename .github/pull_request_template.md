## What does this PR do?

<!-- A short description of the change and why it's needed. Link the issue if there is one: Fixes #123 -->

## How was it tested?

<!-- e.g. npm run test, manual steps in the app -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test` passes
- [ ] New user-facing strings were added to both `zh` and `en` dictionaries
- [ ] No API keys or sensitive data included
